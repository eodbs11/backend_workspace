
-- 2
/* %escape

is null / is not null

IN  

or은 괄호필요()
*/

-- 3
/*
	평균 avg ifnull(..,0)
*/ 
-- 4
/*
	group by 쓴 곳에서는  셀렉트에 group by 에 쓴 컬림이나 그룹함수만 올 수 있음
    
    HAVING과 WHERE의 차이
    
    where은 테이블 전체에 HAVING은 group by 후 그룹화된 테이블에 조건부여
*/

-- 11
/*
	INSERT
*/
-- ****2, 3, 4, 5, 11*****